# RadiAssist Chest API environment configuration
#
# ⚠️ ВАЖНО: Этот файл используется ТОЛЬКО для локальной разработки и тестирования.
# Production API (Docker) работает без этого файла - все необходимые переменные
# уже определены в docker-compose.yml
#
# Для локальной разработки: скопируйте этот файл в .env и настройте пути

# === Переменные для тестовых скриптов (scripts/*.py) ===
# Эти переменные НЕ используются в production API

# Base workspace where models, datasets, and test outputs are stored
RADIASSIST_WORKSPACE=/mnt/pcephfs/lct/radiassist_workspace

# Override test datasets path (defaults to $RADIASSIST_WORKSPACE/datasets/LCT-dataset)
RADIASSIST_TEST_DATA_PATH=/mnt/pcephfs/lct/LCT-dataset

# Location of validation sets (defaults to $RADIASSIST_WORKSPACE/testsets)
RADIASSIST_VALIDATION_PATH=/mnt/pcephfs/lct/radiassist_workspace/testsets

# Optional sample volume and DICOM directory used by example scripts
RADIASSIST_SAMPLE_VOLUME=/mnt/pcephfs/lct/COVID19_1110/studies/CT-0/study_0172.nii.gz
RADIASSIST_SAMPLE_DICOM=/mnt/pcephfs/lct/LCT-dataset/norma_anon

# === Production API переменные ===
# Эти переменные уже определены в docker-compose.yml
# Раскомментируйте только для переопределения значений по умолчанию

# MAX_CONCURRENT_JOBS=2
# PROCESSING_TIMEOUT=600
